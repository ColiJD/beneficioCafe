const theme = {
  token: {
    // 🎨 Colores
    colorPrimary: "#34699A",  // Azul
    colorSuccess: "#347433",  // Verde
    colorError: "#770000",    // Rojo
    colorTextBase: "#171717", // Negro
    colorBgBase: "#ffffff",   // Blanco
    colorTextSecondary: "#393E46", // Gris

    // 🖌️ Tipografía
    fontFamily: "var(--font-montserrat), Arial, Helvetica, sans-serif",
    borderRadius: 8,
  },
  components: {
    Typography: {
      fontFamily: "var(--font-roboto), Arial, Helvetica, sans-serif",
    },
  },
};

export default theme;
